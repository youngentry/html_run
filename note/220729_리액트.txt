22.07.29

리엑트 

컴포넌트 = html을 따로 따로 조립할수있는 상태로 만든다
유지 보수를 용이하게 하기 위해
함수에 담아서 이름을 대문자로 표시하기로 함
그대로 받아줄수 없어서 export를 씀
받을 때 import를 쓴다

자료를 주고받을 방법이 없어서 매개변수로 전달(props)
data[]
html에서 data를 가져와서 쓰고싶을 때 data값을 html에서 쓸수있게 넘겨줌
데이터 공유

state[ ] 데이터를 조작하는 함수

데이터->html 일방향적인 데이터 흐름을 함수를 통해 역으로 연결 가능


쉬프트 우클릭 파워쉘
명령 프롬포트
npx create-react-app react0$

dir 현재 폴더에 어떤 폴더(파일) 있는지 보여줌

리턴 안에
프래그먼트<></>태그로 감싸줘야한다
app.js는 index.js 에서랜더링되고있다

npm start 창 띄우기

컴포넌트는 앞에 대문자로 구분한다

const로 선언하고 props에 담아서 전달한다

function Header (props) {
    return (
        <header>
            {props.monga} -> props와 함수이름을 .으로 연결해주면 페이지에 표시
        </header>
    )
}

같은 줄을 여러번 쓰지 않기 위해
{props.member.map(el => <li key={el.id}>{el.name}</li>)}
와 같이 써준다

html을 함수에 넣어서 쓴다
데이터까지는 연결 못함 props에 넣어서 연결한다



반응형

비포애프터 만들고 디스플레이 플렉스, 플렉스 1 하면 양쪽으로 꽉 찬 선이 생김



클래스는 사방팔방 쓸수있기 때문에 이름을 자세하게 붙이는게 좋다
01 02 로 쓰는 방법은 안됨 섹션 위치가 바뀔 수 있기 때문에

한번 시작한 이름 케이스는 끝까지 같은걸로 적어야 한다

피규어 안에서 반복되는것은 01 02로 써도 된다

슬릭 돌릴때 옆으로 가게하는 플렉스 플롯 등 쓰면 안됨
슬라이드 돌릴 박스 안에 돌려지는거 말고(이미지 등) 다른거 넣으면 안됨


클래스가 4개 이상 깊어지는것은 안좋다

슬라이드 사진 4개 이상해야 깜빡이는 현상이 없다 놉 아니었음

플렉스 아니게 하는 거 -> 디스플레이 블록

    text-indent: -2px; 폰트 스타일때문에 들여쓰기처럼 보이는거 보정

slick-dots li 디스플레이 인라인블록 왜지?



반응형에서 메뉴 창이 나오는 속도는 빠른 것이 좋습니다.
100vh화면에서는 아래에 스크롤이 더 있다는 표시를 남깁니다.
tab메뉴는 텍스트가 긁히지 않도록 하고, cursor: pointor; 를 남깁니다.
볼더계단문제?????????
클릭을 필요로 한느 것은 하는 것은 반드시 a태그로 쌉니다.
영어로 디자인을 하기보다는 한글디자인에 익숙해질 수 있도록 합니다.

마우스 위치를 인식하여 슬라이드 이동 방향을 정하는 연출을 할 수 있습니다.(그런데 어떻게???)